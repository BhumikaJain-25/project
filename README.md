Basic Password Manager /SafeKey Password Locker
# 🔐 PySecureVault
A simple command-line password manager built in Python using **Fernet encryption** from the `cryptography` library.  
This project allows you to **securely store and retrieve encrypted passwords** locally.

---

## 📌 Features
- ✔ Generate and store an encryption key  
- ✔ Encrypt passwords using Fernet symmetric encryption  
- ✔ View stored passwords (auto-decrypted)  
- ✔ Add new passwords securely  
- ✔ Simple, beginner-friendly CLI  

---

## 🛠 Requirements
Install dependencies:

```bash
pip install cryptography
